#include "holberton.h"

/**
 * print_str- print to chain of characters.
 * @s: pointer a printear
 * Return: number of characters to print.
 */
int print_str(char *)
{
	char *str;

	str = s;
	int count;

	if (str == NULL)
	str = "(null)";

	return (count);
}
