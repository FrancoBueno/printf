#include "holberton.h"
#include <strlib.h>
/**
 *defining functions
 *
 */
int print_char (va_list_list)
{char a;
a= va_atg (list, int)
	     putchar(a)
		 return (1)
		 }		 
#include "holberton.h"
#include <strlib.h>
/**
*defining functions
*
*/
int print_string (va_list_list)
{ int i;
                     char *s
		     s = v_arg (list char)
		       if (s!)
			 s= ("null")
			   for (i= 0, i < = s[i]!= 0 , i++)
			     _pucthar(s[i]),
			       return (0);
}
